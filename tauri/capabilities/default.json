{
  "$schema": "../gen/schemas/capabilities-schema.json",
  "identifier": "default",
  "description": "SCM Workbench main window — the splash and UI are embedded Tauri assets. The worker's loopback HTTP listener remains transitional compatibility support for standalone-browser flows; the packaged WebView stays on the embedded origin and uses native IPC.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-wb-restart", "allow-wb-rpc", "allow-wb-decklist-import", "allow-wb-save-artifact"]
}

{
  "$schema": "../gen/schemas/capabilities-schema.json",
  "identifier": "default",
  "description": "SCM Workbench main window — no extra capabilities needed",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save", "allow-wb-restart"]
}
